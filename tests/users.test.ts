import { expect, test } from '@playwright/test';

test.describe('Users', () => {
	test.skip('can join', async ({ page }) => {
		// await page.goto('/');
		// expect(await page.textContent('div.notice')).toBe('Generated by AI, curated by humans');
	});

	test('can login', async ({ page }) => {
		await page.goto('/');
		await expect(page.locator('div.notice', { hasText: 'Generated by AI, curated by humans' })).toBeVisible();

		// take screenshot
		await page.screenshot({ path: `playwright.screenshot.png` });
	});
});
